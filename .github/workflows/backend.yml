name: Backend (Supabase)

on:
  push:
    branches: [main, "arena/**"]
    paths:
      - "supabase/**"
  workflow_dispatch:

jobs:
  validate:
    name: Validate schema + functions
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Probe
        run: echo "probe ok"
