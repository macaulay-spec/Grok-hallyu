EXPO_PUBLIC_SUPABASE_URL=https://psmxekrmoltwabefgqpd.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_C6xEGHQxutS_ub-1ebuUDQ_TxVa_-v0

# Catalog (TMDB). Read-only public client credentials — inlined into the bundle at build time.
# Prefer the v4 read access token; the v3 key is used only when no token is set.
# Leave empty to use the project defaults baked into lib/catalog.ts (same convention as Supabase).
EXPO_PUBLIC_TMDB_ACCESS_TOKEN=
EXPO_PUBLIC_TMDB_API_KEY=

# Show the "Preview with demo data" door on the welcome screen (demo builds). Off by default.
EXPO_PUBLIC_SHOW_DEMO=1
